---
name: Button
route: /components/Button
menu: Components
---

import { Playground, Props } from 'docz'

import { Grid, Button, Badge, TopicBadge } from '../components'

# Button

Button is a box which one can click. Buttons can be disabled if the user has to complete something first, e.g. fill out an input.

## Properties

<Props of={Button} />

## Basic usage

<pre>
  <Button onClick={() => alert('Hello World!')}>Click me!</Button>
</pre>

## Advanced?

<Playground>
  <Button
    border="1px solid textSecondary"
    borderOnHover="brandGreen"
    colorOnHover="brandOrange"
    bg="green" 
    color="black"
    width="auto" 
    p="s" 
    my="l" 
    textAlign="center"
    activeColor="white"
    transition="slow"
    onClick={() => alert('Hello World!')}
  >Hover me, and I change colour!</Button>
  <Badge>Badge</Badge>
  <TopicBadge my="l" color="white" activeColor="black">Cool ToPic</TopicBadge>
</Playground>

